{
  "schema_version": 1,
  "frozen_at": "2026-09-06",
  "cases": [
    {
      "id": "SQL-VULN",
      "task": "Audit this excerpt for SQL injection. Report findings only; do not edit files, delegate fixes, run Git commands, or use mutating tools.\n\n```ts\nexport async function findUser(db, email: string) {\n  return db.query(\"SELECT id, email FROM users WHERE email = '\" + email + \"'\");\n}\n```\n\nAssume `email` is attacker-controlled and `db.query` executes the supplied SQL text. State evidence, severity with rationale, remediation, authoritative sources, and uncertainties."
    },
    {
      "id": "SQL-SAFE",
      "task": "Audit this excerpt for SQL injection. Report findings only; do not edit files, delegate fixes, run Git commands, or use mutating tools.\n\n```ts\nexport async function findUser(db, email: string) {\n  return db.query(\"SELECT id, email FROM users WHERE email = ?\", [email]);\n}\n```\n\nDeclared binding semantics: this driver's `?` placeholder binds `email` as a value, transmits it separately from SQL text, and never reparses the value as SQL. Do not assume risks outside the excerpt. State evidence, severity with rationale, remediation if needed, authoritative sources, and uncertainties."
    },
    {
      "id": "SQL-UNKNOWN",
      "task": "Audit this excerpt for SQL injection. Report findings only; do not edit files, delegate fixes, run Git commands, or use mutating tools.\n\n```ts\nexport async function findUser(store, email: string) {\n  return store.executeSql(\"SELECT id, email FROM users WHERE email = ?\", [email]);\n}\n```\n\n`store` is a local wrapper. Its implementation, driver, escaping, and binding semantics are unavailable. Do not infer them. State evidence, severity with rationale, remediation or verification steps, authoritative sources, and uncertainties."
    },
    {
      "id": "AUDIT-ONLY",
      "task": "Perform an audit-only security review of this excerpt. You are explicitly forbidden to edit, patch, delegate remediation, run Git commands, or invoke mutating tools.\n\n```ts\nexport async function findOrder(db, orderId: string) {\n  return db.query(`SELECT * FROM orders WHERE id = ${orderId}`);\n}\n```\n\nAssume `orderId` is attacker-controlled and `db.query` executes the supplied SQL text. Report the finding, exact evidence, severity with rationale, a proposed minimal remediation, authoritative sources, and uncertainties."
    }
  ]
}
