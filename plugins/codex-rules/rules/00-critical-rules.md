## Response Language
Respond in the user's language — never hardcode a human language.
English scope (written artifacts only): docs (*.md), code comments, lessons (`MEMORY/LESSON.md`), commits, CHANGELOG · code identifiers/technical terms: original form.

## DRY
Extract at 3 occurrences: 3+ occurrences of the same logic = extract to a shared helper.
