---
name: test
description: "Test commit. Use when committing new tests, updated tests, coverage changes, unit tests, or specs."
---

# Test Commit

## Preflight

Inspect test changes before acting:

```bash
git status
git diff --stat -- '*.test.*' '*.spec.*' '__tests__/*' 'test/*' 'tests/*'
```

## Instructions

Create a test commit.

1. Stage test files:
   - `*.test.*`, `*.spec.*`
   - `__tests__/*`, `test/*`, `tests/*`
2. Analyze test files to determine scope
3. Generate commit message:
   - If the user provides context: `test(scope): {user context}`
   - If empty: generate from test names
4. Format: `test(scope): imperative description`
5. **NO AI SIGNATURE**: Never add Co-authored-by, Generated by, or any AI mention
6. Ask confirmation before committing
7. Execute and show result
8. Execute the `post-commit` skill (CHANGELOG + version bump + git tag)

## Related skills

`commit`, `post-commit`, `commit-detection`.

## Skill routing metadata

related-skills: commit, post-commit, commit-detection
