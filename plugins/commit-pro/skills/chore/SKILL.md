---
name: chore
description: "Chore commit. Use when committing maintenance, dependencies, configuration, cleanup, or housekeeping changes."
---

# Chore Commit

## Preflight

Inspect the current state before acting:

```bash
git status
git diff --stat
```

## Instructions

Create a maintenance/chore commit.

1. Stage all changes: `git add -A`
2. Determine scope from files:
   - `package.json` → `deps`
   - Config files → `config`
   - Linting → `lint`
3. Generate commit message:
   - If the user provides context: `chore(scope): {user context}`
   - If empty: generate description
4. Format: `chore(scope): imperative description`
5. **NO AI SIGNATURE**: Never add Co-authored-by, Generated by, or any AI mention
6. Ask confirmation before committing
7. Execute and show result
8. Execute the `post-commit` skill (CHANGELOG + version bump + git tag)

## Related skills

`commit`, `post-commit`, `commit-detection`.

## Skill routing metadata

related-skills: commit, post-commit, commit-detection
